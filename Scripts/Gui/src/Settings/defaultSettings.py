"""
Contains a dictionary of the system default settings of the program.
It will also function as a failsafe when any value isn't able to be read, or if the settings file is missing.
Keeping all the values in one place will also make it possible to restore to system defaults really easily.
"""

defaults = {
    "Side Bar Font Size" : 50
}

names = {
    "Organization" : "L-Egant",
    "App" : "Push-It"
}